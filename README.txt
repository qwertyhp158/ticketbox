# This script is to auto login to ticketbox.vn and go to Born Pink concert page at exactly 12:00AM
# This will not automatically buy the ticket

- Run this in cmd to install required lib

pip install -r requirements.txt

- Run the script at ~11:58AM for best result